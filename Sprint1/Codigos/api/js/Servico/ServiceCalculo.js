/*const { default: aeronave } = require("../Models/aeronave");
const { default: Conf } = require("../Models/Conf");

const aviao = new aeronave(document.getElementById('modelo_de_aeronave').value, ..., ParseInt(document.getElementById('peso').value), ...)
const config = new conf() 

//modelo_de_aeronave, motor, certificacao, peso, reversor, overspeed, flap_de_pouso */